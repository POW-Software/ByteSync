<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:inventories="clr-namespace:ByteSync.ViewModels.Sessions.Inventories"
             xmlns:misc="clr-namespace:ByteSync.Views.Misc"
             xmlns:localizations="clr-namespace:ByteSync.Services.Localizations"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="200"
             x:Class="ByteSync.Views.Sessions.Inventories.InventoryLocalStatusView">

    <Design.DataContext>
        <inventories:InventoryLocalStatusViewModel />
    </Design.DataContext>

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0 6" MaxWidth="1300">
        <!-- Status block -->
        <StackPanel Orientation="Vertical" Height="60"
                    HorizontalAlignment="Center" VerticalAlignment="Center" Margin="24,6" MaxWidth="600">
            <misc:ActivityIndicator Width="80" Height="60" Margin="0"
                                    IsVisible="{Binding IsLocalInventoryInProgress}" />

            <misc:Icon FontSize="60"
                       Value="{Binding LocalMainIcon}"
                       Foreground="{Binding LocalMainIconBrush}"
                       IsVisible="{Binding !IsLocalInventoryInProgress}"
                       Margin="0" VerticalAlignment="Center" HorizontalAlignment="Center" />

        </StackPanel>

        <Label Height="30" VerticalContentAlignment="Center"
               Content="{Binding LocalMainStatusText}"
               HorizontalContentAlignment="Center" FontWeight="Bold" Margin="6" />

        <!-- Times grid -->
        <Grid HorizontalAlignment="Center" Margin="3 12 3 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="{localizations:Loc InventoryProcess_Start}"
                   Classes="InventoryStatusDescription" />
            <Border Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Classes="Badge">
                <TextBlock Text="{Binding StartDateTime, FallbackValue='', StringFormat={}{0:G}}" />
            </Border>

            <Label Grid.Row="1" Grid.Column="0" Content="{localizations:Loc InventoryProcess_ElapsedTime}"
                   Classes="InventoryStatusDescription" />
            <Border Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Classes="Badge">
                <TextBlock Text="{Binding ElapsedTime, StringFormat={}{0:c}}" />
            </Border>

            <Label Grid.Row="2" Grid.Column="0" Content="{localizations:Loc InventoryProcess_RemainingTime}"
                   Classes="InventoryStatusDescription" />
            <Border Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" Classes="Badge">
                <TextBlock Text="{Binding RemainingTime, StringFormat={}{0:c}}" />
            </Border>

            <Label Grid.Row="3" Grid.Column="0" Content="{Binding EstimatedProcessEndName}"
                   Classes="InventoryStatusDescription" />
            <Border Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" Classes="Badge">
                <TextBlock Text="{Binding EstimatedEndDateTime, StringFormat={}{0:G}}" />
            </Border>
        </Grid>
    </StackPanel>
</UserControl>